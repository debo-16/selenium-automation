name: Run changed Selenium tests

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      browser:
        description: 'Choose browser'
        default: 'chrome'
        required: true
        type: string

jobs:
  run-selenium:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Get changed test files
        id: changed-files
        run: |
          git fetch origin main
          changed_files=$(git diff --name-only origin/main HEAD | grep "^test/.*\.js$" || true)
          echo "Changed test files:"
          echo "$changed_files"
          # Replace newlines with spaces for safe parsing
          changed_files="${changed_files//$'\n'/ }"
          echo "files=$changed_files" >> $GITHUB_OUTPUT

      - name: Run changed Selenium tests
        if: steps.changed-files.outputs.files != ''
        run: |
          echo "📁 Files to run: ${{ steps.changed-files.outputs.files }}"
          set -e
          for file in ${{ steps.changed-files.outputs.files }}; do
            echo "🧪 Running test: $file"
            node "$file" --browser="${{ github.event.inputs.browser || 'chrome' }}"
            echo "✅ Finished test: $file"
          done
